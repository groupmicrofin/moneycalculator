<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.groupmicrofin.moneycalc.moneycalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".GroupRegistrationActivity"
            android:label="@string/title.grpReg" />
        <activity
            android:name=".MeetingEntryActivity"
            android:label="@string/title.meetEntry" />
        <activity
            android:name=".AccountRegistrationActivity"
            android:label="@string/title.acctReg" />
        <activity android:name=".LoginAcivtiy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/lable.grpActivity" android:name=".GroupActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".meeting_Activity"></activity>
    </application>

</manifest>